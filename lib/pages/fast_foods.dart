import 'package:flutter/material.dart';

class FastFodsPages extends StatelessWidget {
  const FastFodsPages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text("FAst foods"),);
  }
}